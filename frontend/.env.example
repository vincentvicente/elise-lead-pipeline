# Frontend env vars (Vite-prefixed). Copy to .env.local for local dev.

# Backend API base URL
# - Local dev: leave empty so Vite's dev proxy at /api → http://localhost:8000 takes over
# - Production: full URL of the deployed FastAPI server
VITE_API_BASE_URL=
